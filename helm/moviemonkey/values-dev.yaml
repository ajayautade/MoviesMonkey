# Development overrides
replicaCount: 1

resources:
  requests:
    memory: "32Mi"
    cpu: "25m"
  limits:
    memory: "64Mi"
    cpu: "50m"

autoscaling:
  enabled: false

ingress:
  hosts:
    - host: moviemonkey.dev.local
      paths:
        - path: /
          pathType: Prefix
